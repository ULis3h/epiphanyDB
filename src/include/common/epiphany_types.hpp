#include <stddef.h>

typedef unsigned long long Tid; // table id.

typedef unsigned long long Rid; // record id.

typedef unsigned long long Iid; // index id.

typedef signed long long Pgno; // page number


/** 通用数据类型. */
typedef size_t epiphany_size_t;

typedef char epiphany_int8;

typedef short epiphany_int16;

typedef int epiphany_int32;

typedef long long epiphany_int64;

typedef unsigned char epiphany_uint8;

typedef unsigned short epiphany_uint16;

typedef unsigned int epiphany_uint32;

typedef unsigned long long epiphany_uint64;

